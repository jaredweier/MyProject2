[package]
name = "scheduler_core"
version = "0.1.0"
edition = "2021"
description = "Dodgeville PD Scheduler — high-performance scheduling math (PyO3)"

[lib]
name = "scheduler_core"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module", "abi3-py39"] }
