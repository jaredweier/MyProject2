---
description: Ruff lint and format (free OSS gate)
---

Run terminal only:

```bash
python dev.py lint
```

If auto-fixable issues:

```bash
python dev.py lint --fix
```

Requires: `pip install -r requirements-dev.txt`
