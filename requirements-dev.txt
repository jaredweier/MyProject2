# Optional dev/CI tooling — pip install -r requirements-dev.txt
# Runtime deps remain in requirements.txt only.
ruff>=0.15.22
pyspellchecker>=0.9.0
pre-commit>=4.6.1
pip-audit>=2.10.1
codespell>=2.4.3

# Optional math / E2E / property testing (not required for ship gate)
# ortools>=9.10
# playwright>=1.40
# hypothesis>=6.100
