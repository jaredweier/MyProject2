"""Legacy CustomTkinter package (assets, branding, helpers).

Primary product UI is the NiceGUI Duty Console in `gui/` — run via `main.py`.
This package remains for shared assets/helpers and transitional tooling.
"""
