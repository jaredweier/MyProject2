"""CLI command modules — thin wrappers over logic.*.

Entry point remains root ``cli.py`` (argparse + dispatch).
Handlers: ``roster_cmds``, ``request_cmds``, ``swap_cmds``, ``export_cmds``,
``ops_cmds``, ``bid_cmds``, ``payroll_cmds``, ``user_cmds``, etc.
"""
